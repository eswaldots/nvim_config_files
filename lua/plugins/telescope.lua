return {
  "nvim-telescope/telescope.nvim",
  opts = {
    defaults = {
      file_ignore_patterns = {
        "node_modules",
        "%.git/",
      },
      -- You can add more default settings here if needed
    },
    -- You can configure pickers or extensions here if needed
  },
}
